<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp"
    android:id="@+id/layoutMain" tools:context=".ui.SyncTaskFragment"
    android:clickable="true">


    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:text="Setting"
        android:layout_alignParentEnd="true" />

    <ToggleButton
        android:id="@+id/bnEnableBT"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView2"
        android:layout_centerHorizontal="true"
        android:textOff="Enable Bluetooth"
        android:textOn="Disable Bluetooth"
        android:text="ToggleButton" />

    <ImageButton
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@null"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"
        android:padding="0dp"
        android:id="@+id/bnAddReceiver"
        android:src="@drawable/download"
        android:layout_gravity="center_horizontal"
        android:layout_below="@+id/textView2"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="73dp" />

    <TextView
        android:id="@+id/tvBT1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/bnAddReceiver"
        android:layout_below="@+id/bnAddReceiver"
        android:layout_marginTop="31dp"
        android:text="Bluetooth" />


</RelativeLayout>
